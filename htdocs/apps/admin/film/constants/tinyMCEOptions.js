

/* globals require */

(function (factory) {

    'use strict';

    define([], factory);

})(function () {

    'use strict';

    return {
        selector: "div",
        inline: false,
        plugins : 'advlist autolink image lists charmap preview media',
        skin: 'lightgray',
        theme : 'modern'
    }
});
